{
    "terminal.integrated.profiles.linux": {
        "quad_ocs2": {
            "path": "/bin/bash",
            "args": [
                "-c",
                "if [ -f ${workspaceFolder}/source_ws.sh ]; then source ${workspaceFolder}/source_ws.sh; fi; exec bash"
            ]
        }
    },

    "terminal.integrated.defaultProfile.linux": "quad_ocs2",

    "cmake.sourceDirectory": "${workspaceFolder}"
}